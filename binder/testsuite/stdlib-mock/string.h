#ifndef MOCK_STRING_H
#define MOCK_STRING_H

#include <stdint.h>

size_t strlen(const char *s) { return 0; }

int ffsl(long int i) { return 0; }
int ffsll(long long int i) { return 0; }

#endif // MOCK_STRING_H
