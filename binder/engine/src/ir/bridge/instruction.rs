/// An adapted representation of an LLVM instruction
#[derive(Eq, PartialEq)]
pub enum Instruction {
    // TODO: define the instructions
}

/// An adapted representation of an LLVM terminator
#[derive(Eq, PartialEq)]
pub enum Terminator {
    // TODO: define the terminator
}
